// TypeScript fallback declaration. Metro resolves the platform-specific files
// for native and web bundles before this file at runtime.
export const getReactNativePersistence = (storage: unknown): any => storage;
